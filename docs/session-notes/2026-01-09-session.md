# Session Notes - 2026-01-09

## Context

We paused work on the **Order Management Redesign** (see `docs/architecture/order-management-redesign.md`) to focus on ensuring existing functionality works correctly. The goal is to stabilize the codebase before continuing with the redesign, so the new pending_actions system will integrate cleanly.

## Order Management Progress (Paused)

Completed:
- Database models: `PendingActionModel`, `PendingActionFieldModel` in `shared/database/models.py`
- Domain types: `shared/types/pending_actions.py`
- Repositories: `pending_action.py`, `pending_action_field.py`
- Service skeleton: `features/order_management/service.py` (method signatures only)

Not yet implemented:
- Service method implementations
- Field transform utilities (`output channel -> order field` mapping)
- API endpoints
- Frontend integration

## Fixes Applied This Session

1. **Logger `monitor` attribute error** - Called `setup_logger_class()` early in `app.py` before any loggers are created

2. **Pydantic forward reference (`EtoRunDetailView`)** - Moved stage detail types (`EtoRunExtractionDetailView`, `EtoRunPipelineExecutionDetailView`, `EtoRunPipelineExecutionStepDetailView`) from `eto_runs.py` to `eto_sub_runs.py` to eliminate circular import

3. **`EtoRunUpdate` dict vs Pydantic model** - Fixed `api/routers/eto_runs.py` to create `EtoRunUpdate` model instead of passing plain dict

4. **`dataclasses.replace()` on Pydantic model** - Fixed `pipelines/service.py` to use `step.model_copy(update={...})` instead of `replace(step, ...)`

5. **`ExtractedFieldData` not subscriptable** - Fixed `eto_runs/service.py` to use attribute access (`result.name`) instead of dict subscript (`result["name"]`)

6. **`ExtractedFieldData` objects passed where dicts expected** - Fixed `eto_runs/service.py` to convert to dicts with `model_dump()` before passing to `EtoSubRunExtractionUpdate`

7. **SSE shutdown simplification** - Removed manual shutdown coordination from `eto_events.py` and `order_events_old.py`. SSE generators now just handle `CancelledError` gracefully. Server shutdown still shows a timeout warning but functions correctly.

8. **Import path fix** - Fixed `from server.src.shared.events.order_events_old` to `from shared.events.order_events_old` in `app.py`

## Outstanding Issues (Checklist)

### High Priority - Blocking Functionality

- [ ] **Frontend: Module `module_id` not being sent**
  - Location: Frontend pipeline builder
  - Issue: When dropping a module onto the canvas, frontend sends `module_ref: 'undefined:1.0.0'` instead of `module_id: <integer>`
  - Cause: Backend was refactored to use integer `module_id` (database PK) instead of string `module_ref` (identifier:version), but frontend wasn't updated
  - Fix: Frontend needs to store and send `module_id` from the module catalog response

### Medium Priority - UX Issues

- [ ] **Sub-run detail modal reprocessing**
  - Make reprocessing work within the sub-run detail modal
  - After reprocessing, redirect user to ETO run detail page for that sub run

### Low Priority - Cosmetic

- [ ] **SSE shutdown timeout warning**
  - Server shows "Cancel 1 running task(s), timeout graceful shutdown exceeded" on shutdown with active SSE connections
  - Not blocking - server shuts down correctly, just not gracefully
  - Options: Add timeout to queue.get(), increase uvicorn shutdown timeout, or accept the warning

## Key Files Modified

- `server/src/app.py` - Logger setup, SSE shutdown removal
- `server/src/shared/types/eto_runs.py` - Removed stage detail types, added import from eto_sub_runs
- `server/src/shared/types/eto_sub_runs.py` - Added stage detail types
- `server/src/shared/events/eto_events.py` - Simplified, removed shutdown logic
- `server/src/shared/events/order_events_old.py` - Simplified, removed shutdown logic
- `server/src/shared/logging.py` - Made `setup_logger_class()` idempotent
- `server/src/api/routers/eto_runs.py` - Fixed EtoRunUpdate usage, simplified SSE generator
- `server/src/features/eto_runs/service.py` - Fixed ExtractedFieldData handling
- `server/src/features/pipelines/service.py` - Fixed dataclasses.replace() -> model_copy()

## Next Session

1. Fix the frontend module_id issue (high priority - blocks pipeline builder)
2. Test full ETO run flow end-to-end
3. Resume Order Management implementation once core functionality is stable
