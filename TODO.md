# Front End improvements:

## Template Creation:
 ### ETO Runs Screen:
 - View button can be removed, as template building allows for necessary functionality of viewing objects.
 ### Modal: 
 - Remove status of eto run in the header, as the template is being created based on a certain pdf, not an eto run.
 #### Select static objects portion:
 - Must be some indication for selected objects when their type is not shown. Possibly have them in some other color (gray?), but still have selection for non shown objects disabled.
 - Clear selected objects for template creation when cancelled.
 - Add textual popup to next button when hovered over to indicate that template name is needed.
 #### Assign field extraction labels:
 - Instead of having a simple counter in the template information, have a list of the currently created extraction fields, that you can click on to view
 - Remove draw new field area button when viewing extraction field details.
 - Remove instructions box in the template information card.
 - Allow for the user to click and drag to create new extraction fields, without having to press draw new field area.
 - Allow for the user to draw boxes back and left from the starting point, instead of just starting from the top left.